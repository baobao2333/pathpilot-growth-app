# Project Background
This project is a personal growth application in `F:\growth`.
The requested target architecture is Next.js + React with a canvas layer based on Fabric.js or Konva.

# Working Rules
- Respond to the user in Chinese by default.
- Keep code, code comments, commit messages, and other technical artifacts in English.
- Before coding, inspect the real interfaces and existing conventions.
- Do not add tests unless the user explicitly asks for them.
- Keep changes scoped to the requested application build.
- Do not run dependency installation without a concrete need.

# Prohibited
- Do not refactor unrelated files.
- Do not delete files unless explicitly requested.
- Do not introduce extra abstractions, layers, or files without a clear need.

# Preserve During Compression
When long conversations are automatically compressed, preserve in priority order:
1. Architecture decisions and the reasons behind them
2. Which files changed and what changed
3. Current progress status
4. Unfinished TODOs
